// Main environment configuration (combines server and client)
export { env } from './env';

// Server-only configuration with hierarchical structure
export { serverConfig } from './server-config';

// Public/client-side configuration with hierarchical structure
export { publicConfig } from './public-config';